name: Deploy Google Gmail MCP

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "google-gmail/**"
      - ".github/workflows/deploy-google-gmail.yml"

jobs:
  deploy:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: google-gmail

    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Cache Bun dependencies
        uses: actions/cache@v4
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-google-gmail-${{ hashFiles('google-gmail/bun.lock', 'google-gmail/package.json') }}
          restore-keys: ${{ runner.os }}-bun-google-gmail-

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Type check
        run: bun run check
        continue-on-error: true

      - name: Deploy to Cloudflare Workers
        run: bunx wrangler deploy
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
