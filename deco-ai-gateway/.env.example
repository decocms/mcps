# OpenRouter Management Key (create at https://openrouter.ai/settings/provisioning-keys)
# Used ONLY to create per-org API keys. NOT used for LLM calls.
OPENROUTER_MANAGEMENT_KEY=

# Supabase (table: llm_gateway_connections)
# IMPORTANT: use the SERVICE ROLE key, not the anon key.
# The service_role key bypasses RLS and is required for internal server access.
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=

# AES-256 key to encrypt API keys in Supabase (32 bytes = 64 hex chars)
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY=

# HyperDX (optional — if set, logs are sent to HyperDX in addition to stdout)
# https://www.hyperdx.io/
HYPERDX_API_KEY=

# Log level: debug | info | warn | error (default: info)
LOG_LEVEL=info
