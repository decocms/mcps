{
  "name": "whatsappagent",
  "version": "1.0.0",
  "description": "WhatsApp MCP",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run scripts/dev.ts",
    "dev:link": "deco link -p 8003 -- PORT=8003 bun run dev",
    "check": "tsc --noEmit",
    "build:server": "NODE_ENV=production bun build server/main.ts --target=bun --outfile=dist/server/main.js",
    "build": "bun run build:server"
  },
  "dependencies": {
    "@decocms/bindings": "^1.1.3",
    "@decocms/runtime": "1.1.3",
    "@upstash/redis": "^1.36.1",
    "ai": "^6.0.50",
    "hono": "^4.11.3",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@decocms/mcps-shared": "1.0.0",
    "deco-cli": "0.28.5",
    "typescript": "5.9.3"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}