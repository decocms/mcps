// This file is auto-generated by @hey-api/openapi-ts

import type { Client, Options as Options2, TDataShape } from './client';
import { client } from './client.gen';
import type { GetAutocompleteSuggestionsData, GetAutocompleteSuggestionsErrors, GetAutocompleteSuggestionsResponses, GetBannersByFacetsData, GetBannersByFacetsErrors, GetBannersByFacetsResponses, GetCorrectionSearchData, GetCorrectionSearchErrors, GetCorrectionSearchResponses, GetFacetsByFacetsData, GetFacetsByFacetsResponses, GetPickupPointAvailabilityByFacetsData, GetPickupPointAvailabilityByFacetsErrors, GetPickupPointAvailabilityByFacetsResponses, GetProductsData, GetProductSearchByFacetsData, GetProductSearchByFacetsErrors, GetProductSearchByFacetsResponses, GetProductsErrors, GetProductsResponses, GetSearchSuggestionsData, GetSearchSuggestionsErrors, GetSearchSuggestionsResponses, GetTopSearchesData, GetTopSearchesErrors, GetTopSearchesResponses } from './types.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean> = Options2<TData, ThrowOnError> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: Record<string, unknown>;
};

/**
 * Get list of the 10 most searched terms
 *
 * Lists the 10 most searched terms in the past 14 days.
 *
 * For more information, check the Intelligent Search [Autocomplete](https://help.vtex.com/docs/tutorials/autocomplete) guide.
 *
 * >ℹ️ **Segment context:** Intelligent Search API (Legacy) previously read `locale` from `segment.cultureInfo` in the VTEX segment cookie. In v1, pass it directly as a query parameter.
 *
 * >ℹ️ **Caching:** Responses are cached for public sales channels. Always read the `Cache-Control` response header at runtime to determine cacheability.
 *
 * ## Permissions
 *
 * This endpoint does not require [authentication](https://developers.vtex.com/docs/guides/authentication) or [permissions](https://help.vtex.com/docs/tutorials/license-manager-resources).
 */
export const getTopSearches = <ThrowOnError extends boolean = false>(options?: Options<GetTopSearchesData, ThrowOnError>) => (options?.client ?? client).get<GetTopSearchesResponses, GetTopSearchesErrors, ThrowOnError>({ url: '/top-searches', ...options });

/**
 * Get list of suggested terms and attributes similar to the search term
 *
 * Lists the suggested terms and attributes similar to the search term.
 *
 * For more information, check the Intelligent Search [Autocomplete](https://help.vtex.com/docs/tutorials/autocomplete) guide.
 *
 * >ℹ️ **Segment context:** Intelligent Search API (Legacy) previously read `locale` from `segment.cultureInfo` in the VTEX segment cookie. In v1, pass it directly as a query parameter.
 *
 * >ℹ️ **Caching:** Responses are cached for public sales channels. Always read the `Cache-Control` response header at runtime to determine cacheability.
 *
 * If `query` is missing or empty, the response is `{ "searches": [] }`.
 *
 * ## Permissions
 *
 * This endpoint does not require [authentication](https://developers.vtex.com/docs/guides/authentication) or [permissions](https://help.vtex.com/docs/tutorials/license-manager-resources).
 */
export const getAutocompleteSuggestions = <ThrowOnError extends boolean = false>(options?: Options<GetAutocompleteSuggestionsData, ThrowOnError>) => (options?.client ?? client).get<GetAutocompleteSuggestionsResponses, GetAutocompleteSuggestionsErrors, ThrowOnError>({ url: '/autocomplete-suggestions', ...options });

/**
 * Get list of suggested terms similar to the search term
 *
 * Lists suggested terms similar to the search term.
 *
 * For more information, check the Intelligent Search [Autocomplete](https://help.vtex.com/docs/tutorials/autocomplete) guide.
 *
 * >ℹ️ **Segment context:** Intelligent Search API (Legacy) previously read `locale` from `segment.cultureInfo` in the VTEX segment cookie. In v1, pass it directly as a query parameter.
 *
 * >ℹ️ **Caching:** Responses are cached for public sales channels. Always read the `Cache-Control` response header at runtime to determine cacheability.
 *
 * If `query` is missing, empty, or whitespace-only, the response is `{ "searches": [] }`.
 *
 * ## Permissions
 *
 * This endpoint does not require [authentication](https://developers.vtex.com/docs/guides/authentication) or [permissions](https://help.vtex.com/docs/tutorials/license-manager-resources).
 */
export const getSearchSuggestions = <ThrowOnError extends boolean = false>(options?: Options<GetSearchSuggestionsData, ThrowOnError>) => (options?.client ?? client).get<GetSearchSuggestionsResponses, GetSearchSuggestionsErrors, ThrowOnError>({ url: '/search-suggestions', ...options });

/**
 * Get attempt of correction of a misspelled term
 *
 * Tries to correct a misspelled term from the search.
 *
 * >ℹ️ **Segment context:** Intelligent Search API (Legacy) previously read `locale` from `segment.cultureInfo` in the VTEX segment cookie. In v1, pass it directly as a query parameter.
 *
 * >ℹ️ **Caching:** Responses are cached for public sales channels. Always read the `Cache-Control` response header at runtime to determine cacheability.
 *
 * If `query` is missing, empty, or whitespace-only, the response is `{"correction": {"correction": false, "misspelled": false, "text": "", "highlighted": ""}}`.
 *
 * ## Permissions
 *
 * This endpoint does not require [authentication](https://developers.vtex.com/docs/guides/authentication) or [permissions](https://help.vtex.com/docs/tutorials/license-manager-resources).
 */
export const getCorrectionSearch = <ThrowOnError extends boolean = false>(options?: Options<GetCorrectionSearchData, ThrowOnError>) => (options?.client ?? client).get<GetCorrectionSearchResponses, GetCorrectionSearchErrors, ThrowOnError>({ url: '/correction-search', ...options });

/**
 * Get list of banners registered for query
 *
 * Lists the banners registered for a given query. Check the [configuring banners documentation](https://help.vtex.com/en/tracks/vtex-intelligent-search--19wrbB7nEQcmwzDPl1l4Cb/4ViKEivLJtJsvpaW0aqIQ5) for a full explanation of the banner feature.
 *
 * >ℹ️ **Segment context:** Intelligent Search API (Legacy) previously read `locale` from `segment.cultureInfo` in the VTEX segment cookie. In v1, pass it directly as a query parameter.
 *
 * >ℹ️ **Caching:** Responses are cached for public sales channels. Always read the `Cache-Control` response header at runtime to determine cacheability.
 *
 * ## Permissions
 *
 * This endpoint does not require [authentication](https://developers.vtex.com/docs/guides/authentication) or [permissions](https://help.vtex.com/docs/tutorials/license-manager-resources).
 */
export const getBannersByFacets = <ThrowOnError extends boolean = false>(options: Options<GetBannersByFacetsData, ThrowOnError>) => (options.client ?? client).get<GetBannersByFacetsResponses, GetBannersByFacetsErrors, ThrowOnError>({ url: '/banners/{facets}', ...options });

/**
 * Search products
 *
 * Lists the active products for a given query.
 *
 * >⚠️ **Fetching a single known product?** Use `GET` [Get product](https://developers.vtex.com/docs/api-reference/intelligent-search-api-v1#get-/products) instead. It skips the search pipeline (lower latency) and caches better.
 *
 * >ℹ️ **Caching:** Responses are cached for public sales channels. Always read the `Cache-Control` response header at runtime to determine cacheability. Responses containing sponsored products are not cached.
 *
 * >ℹ️ **Segment context:** Several parameters on this endpoint replace values previously read from the VTEX segment cookie. See [Migrating from segment](#migrating-from-segment) below.
 *
 * ## Migrating from segment
 *
 * Intelligent Search API (Legacy) reads the following values from the VTEX segment cookie. In v1, pass each one directly as a query parameter:
 *
 * | v1 parameter | Segment source |
 * |:---|:---|
 * | `locale` | `segment.cultureInfo` |
 * | `sc` | `segment.channel` |
 * | `regionId` | `segment.regionId` |
 * | `country` | `segment.countryCode` or `country` key in `segment.facets` |
 * | `zip-code` | `zip-code` key in `segment.facets` |
 * | `coordinates` | `coordinates` key in `segment.facets` |
 * | `pickupPoint` | `pickupPoint` key in `segment.facets` |
 * | `deliveryZonesHash` | `deliveryZonesHash` key in `segment.facets` |
 * | `pickupPointsHash` | `pickupPointsHash` key in `segment.facets` |
 * | `utmSource` | `segment.utm_source` |
 * | `utmCampaign` | `segment.utm_campaign` |
 * | `utmiCampaign` | `segment.utmi_campaign` |
 * | `campaigns` | `segment.campaigns` |
 * | `priceTables` | `segment.priceTables` |
 *
 * All other keys in `segment.facets` must be appended to the URL path as `key/value` pairs.
 *
 * <details>
 * <summary>TypeScript reference implementation</summary>
 *
 * The following TypeScript snippet shows the full translation:
 *
 * ```typescript
 * type Segment = {
 * channel?: string | number
 * regionId?: string
 * countryCode?: string
 * cultureInfo?: string
 * // Semicolon-separated "key=value" string, e.g. "zip-code=22250-040;country=BRA"
 * facets?: string
 * utm_source?: string
 * utm_campaign?: string
 * utmi_campaign?: string
 * campaigns?: string
 * priceTables?: string
 * }
 *
 * const SHIPPING_KEYS = new Set([
 * 'zip-code', 'coordinates', 'country', 'pickupPoint',
 * 'deliveryZonesHash', 'pickupPointsHash',
 * ])
 *
 * function segmentToProductSearchV1(segment: Segment, query?: string): string {
 * const shipping: Record<string, string> = {}
 * const pathFacets: Array<{ key: string; value: string }> = []
 *
 * for (const pair of (segment.facets ?? '').split(';')) {
 * const eq = pair.indexOf('=')
 * if (eq < 0) continue
 * const key = pair.slice(0, eq)
 * const value = pair.slice(eq + 1)
 * if (!key || !value) continue
 *
 * if (SHIPPING_KEYS.has(key)) {
 * shipping[key] = value
 * } else {
 * pathFacets.push({ key, value })
 * }
 * }
 *
 * const params: Record<string, string> = {}
 * const set = (name: string, value?: string | number) => {
 * if (value !== undefined && value !== null && value !== '') {
 * params[name] = String(value)
 * }
 * }
 *
 * set('sc', segment.channel)
 * set('locale', segment.cultureInfo)
 * set('regionId', segment.regionId)
 * set('country', segment.countryCode ?? shipping.country)
 * set('zip-code', shipping['zip-code'])
 * set('coordinates', shipping.coordinates)
 * set('pickupPoint', shipping.pickupPoint)
 * set('deliveryZonesHash', shipping.deliveryZonesHash)
 * set('pickupPointsHash', shipping.pickupPointsHash)
 * set('utmSource', segment.utm_source)
 * set('utmCampaign', segment.utm_campaign)
 * set('utmiCampaign', segment.utmi_campaign)
 * set('campaigns', segment.campaigns)
 * set('priceTables', segment.priceTables)
 *
 * if (query) params.query = query
 *
 * const facetPath = pathFacets.map(f => `${f.key}/${f.value}`).join('/')
 * const search = new URLSearchParams(params).toString()
 *
 * return `https://{accountName}.vtexcommercestable.com.br/api/intelligent-search/v1/product-search${facetPath ? `/${facetPath}` : ''}?${search}`
 * }
 * ```
 *
 * </details>
 *
 * ## Sponsored products (VTEX Ads)
 *
 * The query parameters **`showSponsored`**, **`sponsoredCount`**, **`advertisementPlacement`**, and **`repeatSponsoredProducts`** apply only to stores using [VTEX Ads](https://developers.vtex.com/docs/guides/vtex-ads). Sponsored products are merged into the `products` array.
 *
 * ## Permissions
 *
 * This endpoint does not require [authentication](https://developers.vtex.com/docs/guides/authentication) or [permissions](https://help.vtex.com/docs/tutorials/license-manager-resources).
 */
export const getProductSearchByFacets = <ThrowOnError extends boolean = false>(options: Options<GetProductSearchByFacetsData, ThrowOnError>) => (options.client ?? client).get<GetProductSearchByFacetsResponses, GetProductSearchByFacetsErrors, ThrowOnError>({ url: '/product-search/{facets}', ...options });

/**
 * List filters for a search
 *
 * Lists the possible facets for a given query.
 *
 * >ℹ️ **Caching:** Responses are cached for public sales channels. Always read the `Cache-Control` response header at runtime to determine cacheability.
 *
 * >ℹ️ **Segment context:** Several parameters on this endpoint replace values previously read from the VTEX segment cookie. See [Migrating from segment](#migrating-from-segment) below.
 *
 * ## Migrating from segment
 *
 * Intelligent Search API (Legacy) reads the following values from the VTEX segment cookie. In v1, pass each one directly as a query parameter:
 *
 * | v1 parameter | Segment source |
 * |:---|:---|
 * | `locale` | `segment.cultureInfo` |
 * | `sc` | `segment.channel` |
 * | `regionId` | `segment.regionId` |
 * | `country` | `segment.countryCode` or `country` key in `segment.facets` |
 * | `zip-code` | `zip-code` key in `segment.facets` |
 * | `coordinates` | `coordinates` key in `segment.facets` |
 * | `pickupPoint` | `pickupPoint` key in `segment.facets` |
 * | `deliveryZonesHash` | `deliveryZonesHash` key in `segment.facets` |
 * | `pickupPointsHash` | `pickupPointsHash` key in `segment.facets` |
 *
 * All other keys in `segment.facets` must be appended to the URL path as `key/value` pairs.
 *
 * <details>
 * <summary>TypeScript reference implementation</summary>
 *
 * The following TypeScript snippet shows the full translation:
 *
 * ```typescript
 * type Segment = {
 * channel?: string | number
 * regionId?: string
 * countryCode?: string
 * cultureInfo?: string
 * // Semicolon-separated "key=value" string, e.g. "zip-code=22250-040;country=BRA;brand=acme"
 * facets?: string
 * }
 *
 * const SHIPPING_KEYS = new Set([
 * 'zip-code', 'coordinates', 'country', 'pickupPoint',
 * 'deliveryZonesHash', 'pickupPointsHash',
 * ])
 *
 * function segmentToFacetsV1(segment: Segment, query?: string): string {
 * const shipping: Record<string, string> = {}
 * const pathFacets: Array<{ key: string; value: string }> = []
 *
 * for (const pair of (segment.facets ?? '').split(';')) {
 * const eq = pair.indexOf('=')
 * if (eq < 0) continue
 * const key = pair.slice(0, eq)
 * const value = pair.slice(eq + 1)
 * if (!key || !value) continue
 *
 * if (SHIPPING_KEYS.has(key)) {
 * shipping[key] = value
 * } else {
 * pathFacets.push({ key, value })
 * }
 * }
 *
 * const params: Record<string, string> = {}
 * const set = (name: string, value?: string | number) => {
 * if (value !== undefined && value !== null && value !== '') {
 * params[name] = String(value)
 * }
 * }
 *
 * set('sc', segment.channel)
 * set('locale', segment.cultureInfo)
 * set('regionId', segment.regionId)
 * set('country', segment.countryCode ?? shipping.country)
 * set('zip-code', shipping['zip-code'])
 * set('coordinates', shipping.coordinates)
 * set('pickupPoint', shipping.pickupPoint)
 * set('deliveryZonesHash', shipping.deliveryZonesHash)
 * set('pickupPointsHash', shipping.pickupPointsHash)
 *
 * if (query) params.query = query
 *
 * const facetPath = pathFacets.map(f => `${f.key}/${f.value}`).join('/')
 * const search = new URLSearchParams(params).toString()
 *
 * return `https://{accountName}.vtexcommercestable.com.br/api/intelligent-search/v1/facets${facetPath ? `/${facetPath}` : ''}?${search}`
 * }
 * ```
 *
 * </details>
 *
 * ## Permissions
 *
 * This endpoint does not require [authentication](https://developers.vtex.com/docs/guides/authentication) or [permissions](https://help.vtex.com/docs/tutorials/license-manager-resources).
 */
export const getFacetsByFacets = <ThrowOnError extends boolean = false>(options: Options<GetFacetsByFacetsData, ThrowOnError>) => (options.client ?? client).get<GetFacetsByFacetsResponses, unknown, ThrowOnError>({ url: '/facets/{facets}', ...options });

/**
 * Get product
 *
 * Retrieves a single product by a known identifier. Use this endpoint for product detail pages (PDP) when you have a specific product ID, slug, EAN, SKU ID, or reference.
 *
 * >⚠️ Use this endpoint when you need data for one specific product whose identifier is already known (for example, rendering a PDP from a URL slug). Don't use it to fetch multiple products or to run a search query. Use `GET` [Search products](https://developers.vtex.com/docs/api-reference/intelligent-search-api-v1#get-/product-search/-facets-) for those cases. Misusing this endpoint for listings or search bypasses Intelligent Search ranking and impairs the shopper experience.
 *
 * When called with `field=id` (the default), the product is looked up directly, **skipping the search pipeline**, resulting in lower latency and higher cache-hit likelihood than product search.
 *
 * >ℹ️ **Caching:** Responses are cached for public sales channels. Private sales-channel responses are not cached. Always read the `Cache-Control` response header at runtime to determine cacheability.
 *
 * >ℹ️ **Segment context:** Several parameters on this endpoint replace values previously read from the VTEX segment cookie. See [Migrating from segment](#migrating-from-segment) below.
 *
 * ## Migrating from segment
 *
 * Intelligent Search API (Legacy) reads the following values from the VTEX segment cookie. In v1, pass each one directly as a query parameter:
 *
 * | v1 parameter | Segment source |
 * |:---|:---|
 * | `locale` | `segment.cultureInfo` |
 * | `sc` | `segment.channel` |
 * | `regionId` | `segment.regionId` |
 * | `country` | `segment.countryCode` or `country` key in `segment.facets` |
 * | `zip-code` | `zip-code` key in `segment.facets` |
 * | `coordinates` | `coordinates` key in `segment.facets` |
 * | `pickupPoint` | `pickupPoint` key in `segment.facets` |
 * | `deliveryZonesHash` | `deliveryZonesHash` key in `segment.facets` |
 * | `pickupPointsHash` | `pickupPointsHash` key in `segment.facets` |
 * | `utmSource` | `segment.utm_source` |
 * | `utmCampaign` | `segment.utm_campaign` |
 * | `utmiCampaign` | `segment.utmi_campaign` |
 * | `campaigns` | `segment.campaigns` |
 * | `priceTables` | `segment.priceTables` |
 *
 * <details>
 * <summary>TypeScript reference implementation</summary>
 *
 * The following TypeScript snippet shows the full translation:
 *
 * ```typescript
 * type Segment = {
 * channel?: string | number
 * regionId?: string
 * countryCode?: string
 * cultureInfo?: string
 * // Semicolon-separated "key=value" string, e.g. "zip-code=22250-040;country=BRA"
 * facets?: string
 * utm_source?: string
 * utm_campaign?: string
 * utmi_campaign?: string
 * campaigns?: string
 * priceTables?: string
 * }
 *
 * const SHIPPING_KEYS = new Set([
 * 'zip-code', 'coordinates', 'country', 'pickupPoint',
 * 'deliveryZonesHash', 'pickupPointsHash',
 * ])
 *
 * function segmentToProductsV1(segment: Segment, identifier: string): string {
 * const shipping: Record<string, string> = {}
 *
 * for (const pair of (segment.facets ?? '').split(';')) {
 * const eq = pair.indexOf('=')
 * if (eq < 0) continue
 * const key = pair.slice(0, eq)
 * const value = pair.slice(eq + 1)
 * if (!key || !value) continue
 * if (SHIPPING_KEYS.has(key)) shipping[key] = value
 * }
 *
 * const params: Record<string, string> = {}
 * const set = (name: string, value?: string | number) => {
 * if (value !== undefined && value !== null && value !== '') {
 * params[name] = String(value)
 * }
 * }
 *
 * set('sc', segment.channel)
 * set('locale', segment.cultureInfo)
 * set('regionId', segment.regionId)
 * set('country', segment.countryCode ?? shipping.country)
 * set('zip-code', shipping['zip-code'])
 * set('coordinates', shipping.coordinates)
 * set('pickupPoint', shipping.pickupPoint)
 * set('deliveryZonesHash', shipping.deliveryZonesHash)
 * set('pickupPointsHash', shipping.pickupPointsHash)
 * set('utmSource', segment.utm_source)
 * set('utmCampaign', segment.utm_campaign)
 * set('utmiCampaign', segment.utmi_campaign)
 * set('campaigns', segment.campaigns)
 * set('priceTables', segment.priceTables)
 *
 * params.identifier = identifier
 *
 * const search = new URLSearchParams(params).toString()
 *
 * return `https://{accountName}.vtexcommercestable.com.br/api/intelligent-search/v1/products?${search}`
 * }
 * ```
 *
 * </details>
 *
 * ## Permissions
 *
 * This endpoint does not require [authentication](https://developers.vtex.com/docs/guides/authentication) or [permissions](https://help.vtex.com/docs/tutorials/license-manager-resources) for public sales channels.
 */
export const getProducts = <ThrowOnError extends boolean = false>(options: Options<GetProductsData, ThrowOnError>) => (options.client ?? client).get<GetProductsResponses, GetProductsErrors, ThrowOnError>({ url: '/products', ...options });

/**
 * Get pickup point availability for Delivery Promise
 *
 * Retrieves the list of available pickup points with their IDs, distances, addresses, and business hours. This endpoint returns pickup points sorted by distance from the provided coordinates.
 *
 * >ℹ️ This endpoint is designed for use in [Delivery Promise for headless stores](https://developers.vtex.com/docs/guides/delivery-promise-for-headless-stores) implementations.
 *
 * >ℹ️ **Caching:** Responses are cached for public sales channels. Private sales-channel responses are not cached. Always read the `Cache-Control` response header at runtime to determine cacheability.
 *
 * You can call this endpoint in two ways:
 *
 * - **With country and ZIP code:** Provide `country` and `zip-code` to retrieve pickup points based on location.
 * - **With delivery zones and pickup hashes:** Provide pre-computed `deliveryZonesHash` and `pickupPointsHash` for faster lookup.
 *
 * >ℹ️ **Segment context:** Several parameters on this endpoint replace values previously read from the VTEX segment cookie. See [Migrating from segment](#migrating-from-segment) below.
 *
 * ## Migrating from segment
 *
 * Intelligent Search API (Legacy) reads the following values from the VTEX segment cookie. In v1, pass each one directly as a query parameter:
 *
 * | v1 parameter | Segment source |
 * |:---|:---|
 * | `locale` | `segment.cultureInfo` |
 * | `sc` | `segment.channel` |
 * | `country` | `segment.countryCode` or `country` key in `segment.facets` |
 * | `zip-code` | `zip-code` key in `segment.facets` |
 * | `coordinates` | `coordinates` key in `segment.facets` |
 * | `pickupPoint` | `pickupPoint` key in `segment.facets` |
 * | `deliveryZonesHash` | `deliveryZonesHash` key in `segment.facets` |
 * | `pickupPointsHash` | `pickupPointsHash` key in `segment.facets` |
 *
 * <details>
 * <summary>TypeScript reference implementation</summary>
 *
 * The following TypeScript snippet shows the full translation:
 *
 * ```typescript
 * type Segment = {
 * channel?: string | number
 * countryCode?: string
 * cultureInfo?: string
 * // Semicolon-separated "key=value" string, e.g. "zip-code=22250-040;country=BRA"
 * facets?: string
 * }
 *
 * const SHIPPING_KEYS = new Set([
 * 'zip-code', 'coordinates', 'country', 'pickupPoint',
 * 'deliveryZonesHash', 'pickupPointsHash',
 * ])
 *
 * function segmentToPickupPointAvailabilityV1(segment: Segment): string {
 * const shipping: Record<string, string> = {}
 *
 * for (const pair of (segment.facets ?? '').split(';')) {
 * const eq = pair.indexOf('=')
 * if (eq < 0) continue
 * const key = pair.slice(0, eq)
 * const value = pair.slice(eq + 1)
 * if (!key || !value) continue
 * if (SHIPPING_KEYS.has(key)) shipping[key] = value
 * }
 *
 * const params: Record<string, string> = {}
 * const set = (name: string, value?: string | number) => {
 * if (value !== undefined && value !== null && value !== '') {
 * params[name] = String(value)
 * }
 * }
 *
 * set('sc', segment.channel)
 * set('locale', segment.cultureInfo)
 * set('country', segment.countryCode ?? shipping.country)
 * set('zip-code', shipping['zip-code'])
 * set('coordinates', shipping.coordinates)
 * set('pickupPoint', shipping.pickupPoint)
 * set('deliveryZonesHash', shipping.deliveryZonesHash)
 * set('pickupPointsHash', shipping.pickupPointsHash)
 *
 * const search = new URLSearchParams(params).toString()
 *
 * return `https://{accountName}.vtexcommercestable.com.br/api/intelligent-search/v1/pickup-point-availability?${search}`
 * }
 * ```
 *
 * </details>
 *
 * ## Permissions
 *
 * This endpoint does not require [authentication](https://developers.vtex.com/docs/guides/authentication) or [permissions](https://help.vtex.com/docs/tutorials/license-manager-resources).
 */
export const getPickupPointAvailabilityByFacets = <ThrowOnError extends boolean = false>(options: Options<GetPickupPointAvailabilityByFacetsData, ThrowOnError>) => (options.client ?? client).get<GetPickupPointAvailabilityByFacetsResponses, GetPickupPointAvailabilityByFacetsErrors, ThrowOnError>({ url: '/pickup-point-availability/{facets}', ...options });
