// This file is auto-generated by @hey-api/openapi-ts

export type ClientOptions = {
    baseUrl: 'https://sp.vtex.com/event-api/v1/{accountName}' | (string & {});
};

export type UserIdentification = {
    /**
     * anonymous
     *
     * Identifier related to related to the user, in UUID v4 format without dashes. This information is kept in storage for one year.
     */
    anonymous: string;
    /**
     * session
     *
     * Identifier related to the current navigation, in UUID v4 format without dashes. It is a cookie that lasts for 30 minutes, changing if the user opens another tab in private navigation mode.
     */
    session: string;
};

/**
 * SessionPing
 *
 * Sends an ACK to the API to renew the session server-side. It should be sent every 1 minute.
 */
export type SessionPing = {
    /**
     * session
     *
     * Identifier related to the current navigation, in UUID v4 format without dashes. It is a cookie that lasts for 30 minutes, changing if the user opens another tab in private navigation mode.
     */
    session: string;
    /**
     * anonymous
     *
     * Identifier related to related to the user, in UUID v4 format without dashes. This information is kept in storage for one year.
     */
    anonymous: string;
    /**
     * type
     *
     * Type of event, which can be one of the following: `page.cart`, `page.empty_cart`, `search.query`, `page.confirmation`, `session.ping`, `search.click`, `search.autocomplete.query`, or `search.autocomplete.click`.
     */
    type: string;
    /**
     * agent
     *
     * Identifies whether the request came from a mobile or desktop application. It's used as a filter in the search report.
     */
    agent: string;
};

/**
 * PageCart
 *
 * Sends an event every time the shopper enters the cart page. Each interaction should include all products inside the shopping cart at that time. In case a product is removed, sent the updated card.
 */
export type PageCart = {
    /**
     * session
     *
     * Identifier related to the current navigation, in UUID v4 format without dashes. It is a cookie that lasts for 30 minutes, changing if the user opens another tab in private navigation mode.
     */
    session: string;
    /**
     * anonymous
     *
     * Identifier related to related to the user, in UUID v4 format without dashes. This information is kept in storage for one year.
     */
    anonymous: string;
    /**
     * products
     *
     * Array of objects containing products. If the event type is `page.cart`, the array contains all the products in the cart, with their ID and quantity.  Each interaction should include all products inside the shopping cart at that time. In case a product is removed, sent the updated card. If there are no products in the cart (page.`empty_cart`), the array is empty. If the event type is `page.confirmation`, the array contains all the products that were purchased, with their ID, price and quantity.
     */
    products: Array<CartProduct>;
    /**
     * type
     *
     * Type of event, which can be one of the following: `page.cart`, `page.empty_cart`, `search.query`, `page.confirmation`, `session.ping`, `search.click`, `search.autocomplete.query`, or `search.autocomplete.click`.
     */
    type: string;
};

/**
 * Empty Cart
 *
 * Sends an event if there are no products in the shopping cart at that time.
 */
export type PageEmptyCart = {
    /**
     * session
     *
     * Identifier related to the current navigation, in UUID v4 format without dashes. It is a cookie that lasts for 30 minutes, changing if the user opens another tab in private navigation mode.
     */
    session: string;
    /**
     * anonymous
     *
     * Identifier related to related to the user, in UUID v4 format without dashes. This information is kept in storage for one year.
     */
    anonymous: string;
    /**
     * products
     *
     * Array of objects containing products. If the event type is `page.cart`, the array contains all the products in the cart, with their ID and quantity.  Each interaction should include all products inside the shopping cart at that time. In case a product is removed, sent the updated card. If there are no products in the cart (page.`empty_cart`), the array is empty. If the event type is `page.confirmation`, the array contains all the products that were purchased, with their ID, price and quantity.
     */
    products: Array<CartProduct>;
    /**
     * type
     *
     * Type of event, which can be one of the following: `page.cart`, `page.empty_cart`, `search.query`, `page.confirmation`, `session.ping`, `search.click`, `search.autocomplete.query`, or `search.autocomplete.click`.
     */
    type: string;
};

/**
 * Page Confirmation
 *
 * Sends a confirmation informing the products that were bought.
 */
export type PageConfirmation = {
    /**
     * session
     *
     * Identifier related to the current navigation, in UUID v4 format without dashes. It is a cookie that lasts for 30 minutes, changing if the user opens another tab in private navigation mode.
     */
    session: string;
    /**
     * anonymous
     *
     * Identifier related to related to the user, in UUID v4 format without dashes. This information is kept in storage for one year.
     */
    anonymous: string;
    /**
     * products
     *
     * Array of objects containing products. If the event type is `page.cart`, the array contains all the products in the cart, with their ID and quantity.  Each interaction should include all products inside the shopping cart at that time. In case a product is removed, sent the updated card. If there are no products in the cart (page.`empty_cart`), the array is empty. If the event type is `page.confirmation`, the array contains all the products that were purchased, with their ID, price and quantity.
     */
    products: Array<OrderProduct>;
    /**
     * order
     *
     * Order ID.
     */
    order: string;
    /**
     * type
     *
     * Type of event, which can be one of the following: `page.cart`, `page.empty_cart`, `search.query`, `page.confirmation`, `session.ping`, `search.click`, `search.autocomplete.query`, or `search.autocomplete.click`.
     */
    type: string;
};

/**
 * Search Click
 *
 * Sends an event every time a shopper clicks on a product from a search page.
 */
export type Click = {
    /**
     * type
     *
     * Type of event, which can be one of the following: `page.cart`, `page.empty_cart`, `search.query`, `page.confirmation`, `session.ping`, `search.click`, `search.autocomplete.query`, or `search.autocomplete.click`.
     */
    type: string;
    /**
     * Unique identifier of the clicked product.
     */
    productId: string;
    /**
     * Position of the clicked product on the search results page.
     */
    position: number;
    /**
     * url
     *
     * URL that identifies from which page the event occurred.
     */
    url?: string;
    /**
     * text
     *
     * Query used in the search.
     */
    text: string;
    /**
     * agent
     *
     * Identifies whether the request came from a mobile or desktop application. It's used as a filter in the search report.
     */
    agent?: string;
    /**
     * anonymous
     *
     * Identifier related to related to the user, in UUID v4 format without dashes. This information is kept in storage for one year.
     */
    anonymous: string;
    /**
     * session
     *
     * Identifier related to the current navigation, in UUID v4 format without dashes. It is a cookie that lasts for 30 minutes, changing if the user opens another tab in private navigation mode.
     */
    session: string;
};

/**
 * Search Query
 *
 * Sends a query event every time the shopper makes a full-text search.
 */
export type Query = {
    /**
     * session
     *
     * Identifier related to the current navigation, in UUID v4 format without dashes. It is a cookie that lasts for 30 minutes, changing if the user opens another tab in private navigation mode.
     */
    session: string;
    /**
     * anonymous
     *
     * Identifier related to related to the user, in UUID v4 format without dashes. This information is kept in storage for one year.
     */
    anonymous: string;
    /**
     * url
     *
     * URL that identifies from which page the event occurred.
     */
    url?: string;
    /**
     * agent
     *
     * Identifies whether the request came from a mobile or desktop application. It's used as a filter in the search report.
     */
    agent?: string;
    /**
     * type
     *
     * Type of event, which can be one of the following: `page.cart`, `page.empty_cart`, `search.query`, `page.confirmation`, `session.ping`, `search.click`, `search.autocomplete.query`, or `search.autocomplete.click`.
     */
    type: string;
    /**
     * text
     *
     * Query used in the search.
     */
    text: string;
    /**
     * misspelled
     *
     * Indicates whether the query has a typo (`true`) or not (`false`).
     */
    misspelled: boolean;
    /**
     * match
     *
     * Amount of products retrieved by the search.
     */
    match: number;
    /**
     * operator
     *
     * Identifies the type of operator used on the search. The possible values are: `and`, `or`. Find more details in [this Elastic Search guide](https://www.elastic.co/guide/en/elasticsearch/reference/current/query-dsl-match-query.html).
     */
    operator: string;
};

/**
 * Autocomplete Click
 *
 * Sends an event every time a shopper clicks on a product from an autocomplete search.
 */
export type AutocompleteClick = {
    /**
     * type
     *
     * Type of event, which can be one of the following: `page.cart`, `page.empty_cart`, `search.query`, `page.confirmation`, `session.ping`, `search.click`, `search.autocomplete.query`, or `search.autocomplete.click`.
     */
    type: string;
    /**
     * Unique identifier of the clicked product.
     */
    productId: string;
    /**
     * Position of the clicked product on the search results page.
     */
    position: number;
    /**
     * url
     *
     * URL that identifies from which page the event occurred.
     */
    url?: string;
    /**
     * text
     *
     * Query used in the search.
     */
    text: string;
    /**
     * agent
     *
     * Identifies whether the request came from a mobile or desktop application. It's used as a filter in the search report.
     */
    agent?: string;
    /**
     * anonymous
     *
     * Identifier related to related to the user, in UUID v4 format without dashes. This information is kept in storage for one year.
     */
    anonymous: string;
    /**
     * session
     *
     * Identifier related to the current navigation, in UUID v4 format without dashes. It is a cookie that lasts for 30 minutes, changing if the user opens another tab in private navigation mode.
     */
    session: string;
};

/**
 * Autocomplete Query
 *
 * Sends a query event every time the shopper makes an autocomplete search.
 */
export type AutocompleteQuery = {
    /**
     * session
     *
     * Identifier related to the current navigation, in UUID v4 format without dashes. It is a cookie that lasts for 30 minutes, changing if the user opens another tab in private navigation mode.
     */
    session: string;
    /**
     * anonymous
     *
     * Identifier related to related to the user, in UUID v4 format without dashes. This information is kept in storage for one year.
     */
    anonymous: string;
    /**
     * url
     *
     * URL that identifies from which page the event occurred.
     */
    url?: string;
    /**
     * agent
     *
     * Identifies whether the request came from a mobile or desktop application. It's used as a filter in the search report.
     */
    agent?: string;
    /**
     * type
     *
     * Type of event, which can be one of the following: `page.cart`, `page.empty_cart`, `search.query`, `page.confirmation`, `session.ping`, `search.click`, `search.autocomplete.query`, or `search.autocomplete.click`.
     */
    type: string;
    /**
     * text
     *
     * Query used in the search.
     */
    text: string;
    /**
     * misspelled
     *
     * Indicates whether the query has a typo (`true`) or not (`false`).
     */
    misspelled: boolean;
    /**
     * match
     *
     * Amount of products retrieved by the search.
     */
    match: number;
    /**
     * operator
     *
     * Identifies the type of operator used on the search. The possible values are: `and`, `or`. Find more details in [this Elastic Search guide](https://www.elastic.co/guide/en/elasticsearch/reference/current/query-dsl-match-query.html).
     */
    operator: 'and' | 'or';
};

/**
 * Product information.
 */
export type CartProduct = {
    /**
     * Unique identifier of the product.
     */
    productId: string;
    /**
     * Quantity of the product.
     */
    quantity: number;
};

/**
 * Product information.
 */
export type OrderProduct = {
    /**
     * Unique identifier of the product.
     */
    productId: string;
    /**
     * Price of the product.
     */
    price: number;
    /**
     * Quantity of the product.
     */
    quantity: number;
};

export type PostEventData = {
    body?: SessionPing | PageConfirmation | Click | Query | AutocompleteQuery | AutocompleteClick;
    path: {
        /**
         * Name of the VTEX account. Used as part of the URL.
         */
        accountName: string;
    };
    query?: never;
    url: '/event';
};

export type PostEventResponses = {
    /**
     * No content
     */
    204: void;
};

export type PostEventResponse = PostEventResponses[keyof PostEventResponses];
